# MongoDB Configuration
# In Docker, MONGO_HOST should be the MongoDB container name or host accessible from the Docker network
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USERNAME=
MONGO_PASSWORD=
MONGO_DB_NAME=minemaster

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Secret - Generate a secure random string for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Docker / nginx-proxy Configuration
# VIRTUAL_HOST: The subdomain nginx-proxy will route to this container
# VIRTUAL_PORT: The internal port the app listens on (must match PORT above)
VIRTUAL_HOST=minemaster.yourdomain.com
VIRTUAL_PORT=3001

# Let's Encrypt (if using acme-companion with nginx-proxy)
# LETSENCRYPT_HOST should match VIRTUAL_HOST for automatic TLS certificate
LETSENCRYPT_HOST=minemaster.yourdomain.com
LETSENCRYPT_EMAIL=you@yourdomain.com
